<header class="rereview-hero">
  <div class="rereview-shell">
    <p class="rereview-kicker">A retrospective research series</p>
    <h1>My Papers,<br /><em>Re-reviewed.</em></h1>
    <p class="rereview-deck">
      Published work should survive more than peer review. In this series, I return to my own papers, keep the
      contribution that still holds, and name the questions I did not yet know how to ask.
    </p>
  </div>
</header>

<style>
  .rereview-shell {
    margin: 0 auto;
    max-width: 1160px;
    padding-inline: var(--space-content-inline);
  }

  .rereview-hero {
    background: var(--group-wax);
    border-bottom: 1px solid rgb(var(--text-rgb) / 0.16);
    padding: var(--space-section-xlarge) 0 var(--space-section-large);
  }

  .rereview-kicker {
    color: var(--palette-numerical);
    font-size: var(--type-label);
    font-weight: 800;
    letter-spacing: var(--rereview-kicker-spacing);
    margin: 0 0 var(--rereview-kicker-margin);
    text-transform: uppercase;
  }

  .rereview-hero h1 {
    color: var(--text);
    font-family: var(--font-serif);
    font-size: var(--type-display-page);
    letter-spacing: 0;
    line-height: var(--rereview-hero-leading);
    margin: 0;
  }

  .rereview-hero h1 em {
    color: var(--palette-warm);
    font-style: normal;
  }

  .rereview-deck {
    color: rgb(var(--text-rgb) / 0.72);
    font-size: var(--rereview-deck-size);
    line-height: var(--leading-body);
    margin: var(--space-8) 0 0 auto;
    max-width: var(--rereview-deck-measure);
  }
</style>
